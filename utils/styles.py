"""Minimal inline styles for HireAI components (uses Streamlit native dark theme)."""
# No large CSS blocks — Streamlit dark theme handles the base via .streamlit/config.toml
# Keep this file for any future minimal overrides if needed
STYLES = ""
